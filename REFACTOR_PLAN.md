# 리팩토링 명세서

작성일: 2026-07-02
목적: 2년차 이직용 포트폴리오로 끌어올리기 위한 리팩토링 우선순위 정리 (구현은 본인이 진행)

---

## 0. 현재 수준 진단 (2026-07-29 갱신)

Must Fix 4/5 완료. "동작하는 코드를 만드는 것"과 "그 코드가 의도대로 동작하는지 검증하는 것"의 차이를 보여주는 버그들은 대부분 잡혔음. 남은 항목들은 크래시/보안급은 아니고, 책임 분리·타입 안정성·테스트 성숙도 쪽 개선.

1-5(Gemini → Claude 마이그레이션)는 **의도적으로 드롭**. 사이드 프로젝트에 별도 유료 API 토큰을 쓰는 게 아깝다는 판단 (2026-07-29). 같은 문제(`AnalyzeButton.tsx`의 `JSON.parse` 취약성)를 해결하고 싶으면 Gemini `generationConfig.responseSchema`로 무료로 대체 가능 — 필요해지면 그때 검토.

---

## 1. 꼭 고치기 (Must Fix) — 상태

### 1-1. Gemini API 키 클라이언트 노출 (보안) — ✅ 완료

서버 라우트(`/api/gemini`, `/api/gemini/chat`)로 이전, env `NEXT_PUBLIC_GEMINI_API_KEY` → `GEMINI_API_KEY`(서버 전용)로 정리됨.

### 1-2. `errorReport`가 항상 크래시함 — ✅ 완료

`status`를 파라미터로 받도록 수정, `request.json()` 재호출 제거.

### 1-3. `scene/[id]` POST가 파싱 안 된 stream을 그대로 insert — ✅ 완료 (경로 자체 삭제)

문제의 POST 핸들러 제거. insert는 `result/[id]/route.ts`의 POST에서 `await request.json()`으로 정상 처리.

### 1-4. `useFetchData`가 에러를 통째로 삼킴 — ✅ 완료

`{ data, isLoading, error, status }` 반환, 페이지에서 `status`로 에러/로딩 UI 렌더링.

### 1-5. Gemini → Claude 마이그레이션 — ❌ 드롭 (비용)

사이드 프로젝트에 API 토큰 비용 들이지 않기로 결정. 재검토하지 않음.

---

## 2. 통합 우선순위 (다음 단계)

Must Fix 이후 발견된 항목 정리. 시간이 없으면 **Tier 1만, 여유 있으면 Tier 2까지**가 최종 권장. Tier 3/9번/10~14번은 스킵하거나 별도 페이즈.

### Tier 1 — 꼭 (진짜 버그거나, 비용이 0에 가까움)

| 순위 | 항목 | 소요 | 근거 |
|---|---|---|---|
| 1 | `useHandleChatEvent` 네이밍 버그 수정 (파일명 vs export명 `useChatSession` 불일치) | 20분 | ✅ 완료 |
| 3 | `errorReport` 무한 재귀 가능성 제거 — `/api/http`가 자기 자신을 호출하던 구조 | 1시간 | ✅ 완료 — `/api/http`는 실패 시 `console.error`로 종결, 재귀 경로 제거 |
| 7 | 에러 계약 정리 (`HttpError` 구조화) | 1~2시간 | ✅ 완료 — `errorResponse(message, status)` 헬퍼로 통일 (`src/utils/errorResponse.ts`, `HttpError` 타입 실사용), 모든 API 라우트가 이걸 사용하도록 정리. `errorReport`도 `Request` 객체 의존을 없애고 순수 파라미터를 받도록 재설계해 `AnalyzeButton.tsx`의 인라인 중복 리포팅 제거 |

**Tier 1: 완료.** (진행 중 발견된 회귀 버그 — `NextResponse.json()`에 status 옵션 누락으로 에러 응답이 실제로는 HTTP 200을 반환하던 문제 — 도 같이 수정됨)

### Tier 2 — 여유 되면 (서사 완성도를 높임, 비용도 합리적)

| 순위 | 항목 | 소요 | 근거 |
|---|---|---|---|
| 5 | 도메인 타입 정비 — `type/common.ts`의 `story_id: ""`(리터럴 빈 문자열 타입, 사실상 어떤 실제 값도 못 넣는 타입 구멍), `role: string`(Gemini의 `"user"｜"model"` 유니온으로 좁혀야 함) | 반나절 | 실제 타입 구멍이라 "TS strict 모드를 형식적으로만 켜둔 게 아니다"를 보여줄 수 있음 |

### Tier 3 — 스킵해도 무방 (하이 리스크 대비 로우 리턴)

| 순위 | 항목 | 소요 | 비고 |
|---|---|---|---|
| 4 | E2E `setTimeout(3000)` → `waitForResponse` 교체 | 1시간 | 내부 테스트 안정성일 뿐, 면접에서 설명할 이야기가 약함 |
| 6 | 파생 상태 정리 (중복 상태·매직넘버) | 반나절 | 하이진 수준, 스토리텔링 어려움 |
| 8 | `Button` variant 동작분기 분리 | 1~2시간 | 나쁘지 않지만 우선순위 밀림 |
| 9 | `TextAccordion` 분기 분리 + 폴더명 오타 | 1~2시간 | 오타 수정 수준 |

### 다음 페이즈 (개별 반나절~1.5일, 이번 라운드에서 제외)

| 순위 | 항목 | 소요 | 상태 |
|---|---|---|---|
| ~~2~~ | ~~`chatId`를 `useState`/`useMemo`로 고정~~ | — | ✅ 이미 완료 |
| ~~10~~ | ~~`useFetchData` 제네릭화~~ | — | ✅ 이미 완료 (`useFetchData<T>` 적용됨, `body?: any`만 잔존 — 남은 건 5분짜리 타입 하나뿐이라 Tier 1에 묶어도 됨) |
| 11 | `AnalyzeButton` → 서버 라우트 이전 (책임 분리) | 1.5일 | 가치는 가장 높지만 비용도 가장 큼. Tier 1/2 끝난 뒤 별도 판단 |
| 12 | API 응답 포맷 표준화 | 중~대 | 라우트 전체 손대야 함 |
| 13 | 데이터 페칭 React Query/SWR 교체 | 대 | 사실상 아키텍처 교체 — "돈은 안 쓰지만 시간은 크게 쓰는" 결정, 나중에 따로 논의 |
| 14 | 훅/유틸 단위 테스트(Vitest) 추가 | 중~대 | 처음부터 시작하는 영역 |

---

## 참고: 각 항목이 면접/블로그에서 쓸 수 있는 각도

- 1-1 + (드롭된 1-5 대신) Gemini `responseSchema`: "AI 기능이 있는 프론트엔드에서 API 키를 안전하게 다루는 법 (BFF 패턴)"
- 1-2: "에러 핸들링을 추가했는데 왜 안 잡혔을까 — 에러 처리 코드도 테스트가 필요한 이유"
- 1-4: "에러 상태를 UI까지 제대로 전파하기 — 로딩/에러/빈 상태를 구분해서 설계하기"
- 3 (errorReport 무한 재귀): "에러 리포팅 시스템 자체가 장애 지점이 되지 않게 설계하기"
- 11 (다음 페이즈): "컴포넌트에 몰려있던 책임을 분리하기"
- 13 (다음 페이즈): "직접 만든 fetch 훅의 한계와 React Query로의 전환기"
